#ifndef ENUM_H
#define ENUM_H

enum class TDAType { STACK, QUEUE, LIST, SPECIAL };

enum class TDACardOperation {
    PUSH,
    POP,
    ENQUEUE,
    DEQUEUE,
    INSERT,
    DELETE,
    CHANGETDA
};

#endif // ENUM_H
